source-files-location: src/main/kotlin

source-files:
  - File1.kt

dependencies:

jar-run-config:
  docker-container-name: "my-test-container"
  docker-port: 9090
  wiremock-cl-options:
    - --verbose
    - --record-mappings

